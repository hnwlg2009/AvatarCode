# AvatarCode 多语言功能模块规格说明 (SPEC)

**版本**: 1.0  
**状态**: 草案  
**日期**: 2026-05-18

---

## 1. 功能概述

### 1.1 功能名称
多语言支持模块 (Internationalization Module)

### 1.2 功能描述
为 AvatarCode IDE 提供完整的多语言支持，包括：
- 界面语言切换（中文/英文）
- Electron 主进程菜单多语言
- 渲染进程 UI 组件多语言
- 语言设置持久化
- 语言切换即时生效

### 1.3 目标用户
- 中文开发者
- 英文开发者
- 国际化团队

---

## 2. 功能需求

### 2.1 语言切换功能

#### 2.1.1 语言选择器
- **位置**：设置页面 → 通用设置 → 语言
- **选项**：
  - English (英文)
  - 中文 (简体中文)
- **交互**：点击语言选项即时切换
- **视觉反馈**：当前选中语言高亮显示

#### 2.1.2 语言切换行为
- **即时生效**：切换后所有 UI 文本立即更新
- **菜单更新**：Electron 主进程菜单同步更新
- **持久化**：语言选择保存到 localStorage
- **启动恢复**：下次启动时自动使用上次选择的语言

### 2.2 多语言覆盖范围

#### 2.2.1 渲染进程组件
| 组件 | 翻译内容 | 优先级 |
|------|----------|--------|
| Workspace | 空状态提示、按钮文本 | 高 |
| ChatPanel | 标题、按钮、占位符 | 高 |
| AgentPanel | 标题、标签、按钮 | 高 |
| PluginPanel | 标题、搜索、状态 | 高 |
| GitPanel | 标签、按钮、状态 | 高 |
| SettingsPanel | 标题、标签 | 高 |
| EditorSettings | 所有设置项 | 高 |
| GeneralSettings | 语言选择 | 高 |
| GitSettings | 表单标签、按钮 | 高 |
| AppearanceSettings | 主题选项 | 高 |
| APISettings | 表单标签、提示 | 中 |
| MainLayout | 侧边栏标题、工具栏 | 中 |
| FileTree | 空状态提示 | 中 |
| TerminalPanel | 欢迎消息 | 低 |
| TabBar | 关闭按钮 | 低 |

#### 2.2.2 主进程组件
| 组件 | 翻译内容 | 优先级 |
|------|----------|--------|
| 应用菜单 | 所有菜单项 | 高 |
| 对话框 | 按钮文本、标题 | 中 |

### 2.3 翻译文件结构

#### 2.3.1 翻译 key 命名规范
```
{模块}.{功能}.{具体项}
```

示例：
```json
{
  "settings": {
    "language": "Language",
    "langEn": "English",
    "langZh": "中文"
  },
  "chat": {
    "title": "AI Chat",
    "send": "Send"
  }
}
```

#### 2.3.2 翻译文件组织
```
src/i18n/locales/
├── en.json    # 英文翻译
└── zh.json    # 中文翻译

electron/locales/
├── en.json    # 菜单英文翻译
└── zh.json    # 菜单中文翻译
```

### 2.4 语言检测与回退

#### 2.4.1 语言检测顺序
1. localStorage 存储的语言设置
2. 浏览器/系统语言设置
3. 默认语言（英文）

#### 2.4.2 回退机制
- 如果翻译 key 不存在，回退到英文
- 如果英文翻译也不存在，显示 key 本身

---

## 3. 非功能需求

### 3.1 性能要求
- 语言切换响应时间 < 100ms
- 翻译文件加载时间 < 50ms
- 不影响应用启动时间

### 3.2 可用性要求
- 语言切换操作简单直观
- 切换后无需重启应用
- 语言选择持久化

### 3.3 可维护性要求
- 翻译文件结构清晰
- 翻译 key 命名规范
- 易于添加新语言

### 3.4 兼容性要求
- 支持 Electron 主进程和渲染进程
- 支持 Windows、macOS、Linux
- 支持中英文字符显示

---

## 4. 用户界面设计

### 4.1 语言选择器 UI
```
┌─────────────────────────────────┐
│ 通用设置                        │
├─────────────────────────────────┤
│ 语言                            │
│ ┌─────────┐ ┌─────────┐        │
│ │ 🇺🇸 English │ │ 🇨🇳 中文 │        │
│ │  (选中)   │ │         │        │
│ └─────────┘ └─────────┘        │
└─────────────────────────────────┘
```

### 4.2 菜单多语言示例
**英文菜单**：
```
File  Edit  View  Window  Help
```

**中文菜单**：
```
文件  编辑  视图  窗口  帮助
```

---

## 5. 技术规格

### 5.1 依赖库
- i18next: ^26.2.0
- react-i18next: ^17.0.8
- i18next-browser-languagedetector: ^8.2.1

### 5.2 API 接口

#### 5.2.1 渲染进程 API
```typescript
// 使用 useTranslation hook
const { t, i18n } = useTranslation();

// 翻译文本
t('settings.language')

// 切换语言
i18n.changeLanguage('zh');
```

#### 5.2.2 主进程 API
```typescript
// 翻译函数
electronI18n.t('menu.file')

// 加载语言
electronI18n.loadTranslations('zh')

// 获取当前语言
electronI18n.getLanguage()
```

#### 5.2.3 IPC 通信
```typescript
// 渲染进程发送语言变更通知
window.electronAPI.send('language-changed', 'zh');

// 主进程接收语言变更通知
ipcMain.on('language-changed', (event, lang) => {
  // 更新菜单语言
});
```

### 5.3 存储格式
```typescript
// localStorage
{
  "avatarcode-language": "zh" | "en"
}
```

---

## 6. 验收标准

### 6.1 功能验收
- [ ] 语言选择器正确显示中英文选项
- [ ] 切换语言后所有 UI 文本更新
- [ ] Electron 菜单语言同步更新
- [ ] 语言选择持久化到 localStorage
- [ ] 应用启动时使用上次选择的语言
- [ ] 翻译缺失时回退到英文

### 6.2 性能验收
- [ ] 语言切换响应时间 < 100ms
- [ ] 翻译文件加载时间 < 50ms
- [ ] 不影响应用启动性能

### 6.3 兼容性验收
- [ ] Windows 平台正常工作
- [ ] macOS 平台正常工作
- [ ] Linux 平台正常工作
- [ ] 中英文字符正确显示

### 6.4 代码质量验收
- [ ] 翻译 key 命名规范
- [ ] 翻译文件结构清晰
- [ ] 组件正确使用 useTranslation
- [ ] 无硬编码 UI 文本

---

## 7. 测试用例

### 7.1 功能测试
| 测试项 | 测试步骤 | 预期结果 |
|--------|----------|----------|
| 语言切换 | 点击中文选项 | 所有 UI 文本切换为中文 |
| 菜单更新 | 切换语言后查看菜单 | 菜单项显示对应语言 |
| 持久化 | 切换语言后重启应用 | 应用使用上次选择的语言 |
| 回退机制 | 访问不存在的翻译 key | 显示英文或 key 本身 |

### 7.2 边界测试
| 测试项 | 测试步骤 | 预期结果 |
|--------|----------|----------|
| 快速切换 | 快速连续切换语言 | 应用正常响应，无崩溃 |
| 无效语言 | 设置无效的语言值 | 回退到默认语言 |
| 翻译缺失 | 删除部分翻译 key | 应用正常显示，回退到英文 |

### 7.3 兼容性测试
| 测试项 | 测试环境 | 预期结果 |
|--------|----------|----------|
| Windows | Windows 10/11 | 功能正常 |
| macOS | macOS 12+ | 功能正常 |
| Linux | Ubuntu 20.04+ | 功能正常 |

---

## 8. 风险与缓解

### 8.1 技术风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| IPC 通信失败 | 菜单语言不同步 | 添加错误处理和重试机制 |
| 翻译文件加载失败 | UI 显示异常 | 添加回退机制和错误提示 |
| 字符编码问题 | 中文显示乱码 | 确保 UTF-8 编码 |

### 8.2 进度风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 组件改造工作量大 | 开发周期延长 | 分阶段实施，优先高优先级组件 |
| 翻译质量不高 | 用户体验差 | 进行翻译审核和用户测试 |

---

## 9. 附录

### 9.1 术语表
- **i18n**: Internationalization，国际化
- **l10n**: Localization，本地化
- **翻译 key**: 用于标识翻译文本的唯一标识符
- **回退语言**: 当翻译缺失时使用的默认语言

### 9.2 参考文档
- [i18next 官方文档](https://www.i18next.com/)
- [react-i18next 官方文档](https://react.i18next.com/)
- [Electron IPC 文档](https://www.electronjs.org/docs/latest/api/ipc-main)
